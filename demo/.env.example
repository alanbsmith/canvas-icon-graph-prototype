# Copy to .env. No real secrets here -- this just points at the search
# server's REST API, which must already be running (see ../README.md,
# `npm run search-server` from the project root) for this demo to work.
VITE_SEARCH_API_BASE=http://localhost:3000
